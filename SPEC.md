# AI Virtual Team Builder — 仕様書

**バージョン**: 1.0  
**作成日**: 2024年7月  
**目的**: どんなプロジェクトにも使える汎用AI従業員チームビルダー

---

## プロダクトビジョン

「知識を入れるだけのAI」ではなく、**役割・人格・思考スタイル・プロジェクトの魂を持つ仮想従業員チーム**を誰でも5分で作れるツール。

> 知識（何を知っているか）× 人格（何者として働くか）＝ 自律的に働く従業員

---

## 技術スタック

```yaml
frontend:
  primary: Streamlit（ローカル優先）
  future: Next.js（WEBデプロイ時に移行可能な設計にすること）

backend:
  framework: FastAPI（Python）
  note: フロントとAPIを分離し、将来のWEBデプロイに備える

data:
  primary: JSONファイル（ローカル）
  future: Supabase（WEBデプロイ時に切り替え可能な設計にすること）
  path: ./data/projects/  # プロジェクトごとにJSONで保存

llm:
  supported:
    - Gemini 2.0 Flash（デフォルト推奨）
    - Claude Sonnet 4.5
    - GPT-4o
  config: .envファイルで切り替え。社員ごとに個別選択も可能

github:
  library: PyGithub
  auth: .envのGITHUB_TOKENで認証

packaging:
  distribution: .env.exampleとsetup.ps1/run.ps1（Windows対応）で社内配布可能な構成
```

---

## データ構造

```json
// data/projects/{project_id}.json
{
  "id": "npo-trust-platform",
  "name": "NPO Trust Platform",
  "owner_context": {
    "motivation": "なぜこのプロジェクトを始めたか（自由記述）",
    "vision": "5年後にどんな状態にしたいか",
    "values": "大切にしている価値観・判断基準",
    "ng_items": "絶対にやってはいけないこと",
    "current_phase": "今どのフェーズにいるか"
  },
  "github_repo": "username/repo-name",
  "employees": [
    {
      "id": "emp-001",
      "name": "鋭子",
      "attribute": "クリティカル",
      "display_name": "鋭子（クリティカル）",
      "specialty": "批判的思考・リスク洗い出し",
      "personality_prompt": "...",
      "knowledge_sources": [
        { "type": "markdown", "path": "./data/knowledge/emp-001/..." },
        { "type": "url", "url": "https://..." }
      ],
      "llm_model": "gemini-2.0-flash",
      "is_active": true,
      "chat_history": []
    }
  ]
}
```

---

## 機能仕様

### 1. プロジェクト管理

- プロジェクトの新規作成・一覧表示・切り替え・削除
- プロジェクト作成時に「オーナー設定（動機・ビジョン・価値観・NG事項）」を入力
- オーナー設定は全従業員の知識ストックに自動で共通注入される

### 2. 従業員管理

- 従業員の新規追加・編集・削除
- 各従業員に設定する項目:
  - 名前（人名形式）
  - 属性（クリティカル / ドリーマー / リアリスト / 専門家 から選択またはカスタム入力）
  - 専門領域（自由記述）
  - 人格プロンプト（手動入力 または 自動生成）
  - 使用LLMモデル（プロジェクトデフォルトを継承 or 個別指定）
  - アクティブ状態（チェックボックスでON/OFF）

### 3. 人格自動生成機能（コア機能）

オーナー設定（動機・ビジョン・価値観）と従業員の専門領域・属性を入力すると、LLMがその組み合わせに最適化された人格プロンプトを自動生成する。

生成後は編集可能。確定するとその従業員の人格プロンプトとして保存される。

**生成プロンプトの構造**:
```
# あなたの役割
あなたは {name}（{attribute}）として、{owner_name} のプロジェクトに参加する {specialty} の専門家です。

# あなたが仕える相手について
{owner_context.motivation}
{owner_context.vision}
{owner_context.values}

# あなたの思考スタイル
{attribute に応じた思考スタイルの指示}

# 行動指針
- 受け身で答えるのではなく、プロとして先回りして指摘・提案・次の一手を示す
- 結論ファーストで前置きを省く
- 情報が不足していると感じたら、逆質問で確認する
- 回答の最後に必ず「次に取るべき具体的な一手」を1つ示す
```

### 4. 知識ストック管理

各従業員に以下のソースを追加・削除できる:

- **Markdownファイル**: ローカルのMDファイルをアップロード
- **テキスト直接入力**: 設計書・メモ等をその場で貼り付け
- **URL**: WebページのURLを指定（スクレイピングしてテキスト化）
- **GitHubファイル**: リポジトリの特定ファイルまたはディレクトリを指定

### 5. GitHub連携

- リポジトリのファイルツリーを表示し、取り込むファイルをチェックボックスで選択
- 選択したファイルの内容を取得し、指定した従業員の知識ストックに追加
- 「全員に追加」ボタンで全従業員に一括追加も可能
- 「知識を更新」ボタンで最新のコードを再取得（差分のみ更新）

### 6. チャット機能

**個別チャット**:
- 従業員を1人選んでチャット
- 会話履歴を保存（セッションをまたいで継続）
- 知識ストックの内容をコンテキストとして自動注入

**全員同時レビュー**:
- トピック（質問・相談内容）を入力
- アクティブな従業員全員が並列でレビューを実行
- 結果をタブ形式で表示（従業員ごとに1タブ）
- 「全員の意見をまとめる」ボタンで統合サマリーを生成

**チェックボックスによる参加選択**:
- 全員同時レビュー時に、参加させる従業員をチェックボックスで選択可能
- 思考スタイル軸（クリティカル・ドリーマー・リアリスト）も個別にON/OFF可能

### 7. エクスポート

- チャット履歴・レビュー結果をMarkdownファイルで出力
- 出力先: `./exports/{project_id}/{date}/`

### 8. 社内配布パッケージ

以下のファイルを同梱し、`.env`ファイルを差し替えるだけで使えるようにする:

```
ai-virtual-team/
├── .env.example        ← APIキー・GitHub認証の設定例
├── setup.ps1           ← pip installを１コマンドで完了（PowerShell用）
├── setup.bat           ← pip installを１コマンドで完了（コマンドプロンプト用）
├── run.ps1             ← FastAPI + Streamlit起動（PowerShell用）
├── run.bat             ← FastAPI + Streamlit起動（コマンドプロンプト用）
├── README.md           ← 5分でセットアップできる手順書（スクリーンショット付き）
└── data/
    └── presets/
        └── npo-trust-platform.json  ← NPO Trust Platform用プリセット
```

---

## 初期プリセット: NPO Trust Platform チーム

ツールインストール直後から使えるサンプルとして同梱する。

### 思考スタイル軸（3名・全プロジェクト共通テンプレート）

| 名前 | 属性 | 思考スタイルの核心 |
| :--- | :--- | :--- |
| 鋭子（クリティカル） | 批判的思考 | 最悪ケース・リスク・設計の穴を先に提示。「なぜ失敗するか」を教える |
| 夢太（ドリーマー） | 拡張的思考 | 制約を無視して可能性を最大化。熱量を増幅させ「もっとこうできる」を教える |
| 実（リアリスト） | 現実的思考 | 工数・優先順位・実行可能性を判断。「では具体的にどうするか」を教える |

### 専門家軸（6名・NPO Trust Platform専用）

| 名前 | 属性 | 専門領域 |
| :--- | :--- | :--- |
| 健一（戦略・広報） | 経営・PR | ビジネスモデル・競合・成長戦略・ブランド・メディア戦略 |
| 律子（法務） | リーガル | 資金決済法・名誉毀損・個人情報保護法・利用規約 |
| 剛（財務） | CFO | 収益モデル・コスト構造・持続可能性・資金調達 |
| 美咲（デザイン） | UI/UX | プロフェッショナルなデザイン原則・ユーザー心理・離脱防止・アクセシビリティ |
| 徹（エンジニア） | テック | システム設計・セキュリティ・技術負債・GCPデプロイ |
| 恵（現場） | NPO業界 | 寄付者心理・NPO現場の実態・社会課題・ボランティア文化 |

---

## 画面構成

```
サイドバー:
  - プロジェクト切り替えセレクト
  - 従業員一覧（チェックボックスでアクティブ切り替え）
  - GitHub連携ボタン
  - 設定（オーナー設定・LLMモデル選択）

メインエリア（タブ切り替え）:
  タブ1: 全員レビュー
    - トピック入力テキストエリア
    - 参加従業員チェックボックス
    - 「全員に聞く」ボタン
    - 結果表示（従業員ごとのタブ）
    - 「統合サマリーを生成」ボタン

  タブ2: 個別チャット
    - 従業員選択セレクト
    - チャット画面（会話履歴表示）
    - 入力フォーム

  タブ3: 従業員管理
    - 従業員一覧テーブル
    - 追加・編集・削除ボタン
    - 人格自動生成ボタン

  タブ4: 知識ストック
    - 従業員選択
    - ソース一覧（追加・削除）
    - GitHub連携UI（ファイルツリー + チェックボックス）

  タブ5: エクスポート
    - 出力対象選択
    - Markdownダウンロードボタン
```

---

## 将来のWEBデプロイ対応方針

- FastAPIのAPIエンドポイントは最初から`/api/v1/`プレフィックスで設計する
- データ層はリポジトリパターンで抽象化し、JSONファイルとSupabaseを切り替え可能にする
- Streamlitの画面コンポーネントは、将来Next.jsへの移植を想定してロジックとUIを分離する
- 認証は将来的にSupabase Authを使うことを想定し、現時点では`.env`の`OWNER_NAME`で識別する
